Place the frontpage hero image here as `hero.jpg`.

Instructions:
1. Save the image you uploaded in the project root to this folder: `frontend/public/images/hero.jpg`.
2. The app uses `/images/hero.jpg` as the frontpage hero. After adding the image, rebuild or restart the frontend dev server if needed.

Notes:
- For best results use a wide, high-resolution image (e.g. 1920x600).
- If you want me to add the image to the repo, upload the file here or grant access and I'll commit it for you.